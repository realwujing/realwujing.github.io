# CTYunOS 2 Kernel Build Environment
# This Dockerfile is minimal - user mapping is handled via docker run --user
FROM ctyunos:22.06-build

# No user creation needed - we use --user flag at runtime
# This allows the container to run with the host user's UID/GID

CMD ["/bin/bash"]
